{
  "name": "tk",
  "version": "2.2.0",
  "description": "Session and engineering flow: kickoff (session open, dispatches the next-steps queue; afk/pack package modes), wrap-up (session close with a versioning gate; afk mode), dispatch (task → execution mechanism), docs-audit (docs vs. code), verify (the item's acceptance criterion as the ruler of the delivery), review (lens campaign: the second pair of eyes on a delivered code or data slice). Site-specific integrations plug in via ~/.claude/tk/<skill>.md extension files.",
  "author": {
    "name": "Tarcisio-Abbud"
  }
}
